/**
 * 
 * Line decoration custom implementation template.
 * implement those methods
 * 
 */

// onStart :: context -> string
const onStart = (context) => {
    return null
}

const onLine = (line, context) => {
    return line
}

const onClose = (context) => {
    return null
}


module.exports = {
    onStart,
    onClose,
    onLine,
}
